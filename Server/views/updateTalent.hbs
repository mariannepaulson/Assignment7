<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <title>Update Talent</title>
</head>

<body>
{{> header}}
<h1>{{pageTitle}}</h1>

<form method='post' action='/updateTalent/{{lname}}'>

<fieldset>
            <p>
            <label>First Name*:</label>
            <input type = "text"
                   value = "{{talent.fname}}"
                   name = "fname" />
            </p>

            <p>
            <label>Middle Name:</label>
            <input type = "text"
                   value = "{{talent.mname}}"
                   name = "mname" />
            </p>

            <p>
            <label>Last Name*:</label>
            <input type = "text"
                   value = "{{talent.lname}}"
                   name = "lname" />
            </p>

            <p>
            <label>Phone number:</label>
            <input type = "text"
                   value = "{{talent.phone}}"
                   name = "phone" />
            </p>

            <p>
            <label>Email address:</label>
            <input type = "text"
                   value = "{{talent.email}}"
                   name = "email" />
            </p>

            <p>
            <label>Jobs of Interest*:</label>
            <input type = "text"
                   value = "{{talent.jobs}}"
                   name = "jobs" />
            </p>

            <p>
            <button type = "submit"
                    value = "Update">
                Update
            </button>
          </p>

        </fieldset>
    </form>

{{> footer}}

</body>
</html>
